<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dummies.invest">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Welcome">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RequestFormActivity"
            android:label="@string/title_activity_request_form"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Welcome">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.dummies.invest.MainActivity" />
        </activity>
        <activity
            android:name=".MoreDetails"
            android:label="@string/title_activity_more_details"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Welcome">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.dummies.invest.MainActivity" />
        </activity>
    </application>

</manifest>